import { FC } from 'react';
import s from './s_pokedex-details.module.scss';

const PokeDetailsPage: FC = () => {
  return (
    <main>
      <section className={s.container}>
        <div className={s.wrapper}> here </div>
      </section>
    </main>
  );
};

export default PokeDetailsPage;
